// const pool = require("../../config/db");

// exports.log = async (userId, action) => {
//   await pool.query(
//     "INSERT INTO audit_logs (user_id, action) VALUES (?,?)",
//     [userId, action]
//   );
// };